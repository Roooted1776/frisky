# Security

Policies: in-app Help → Security (`RedMed-Xcode/RedMed/Help.html`).

Report XSS in `#d=` rendering, Keychain / vault bypass, or owner unlock-gate bypass via a private GitHub advisory on this repo, or help.RedMed@gmail.com. Do not file public issues with live keys or real medical payloads.

Findings: `docs/AUDIT.md`.
