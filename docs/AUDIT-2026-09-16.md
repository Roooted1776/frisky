# RedMed audit sweep — 2026-09-16

**Repo:** [Roooted1776/frisky](https://github.com/Roooted1776/frisky) @ `de1127e` (`main`) plus this branch’s small fixes.  
**Method:** static read of Swift / tapper / Document / CI / docs; `node scripts/test-d-codec.mjs`; live HTTP probes. No iOS Simulator on this Linux VM. No secrets found that needed rotation.  
**Did not:** merge; restore CoreNFC / Write The Band / Load From Band / HealthKit / Associated Domains; flip entitlements; rewrite HIPAA / FDA legal copy.

Parked lockstep (verified, left parked):

| Switch | Evidence |
|--------|----------|
| CoreNFC | `AppConfig.nfcHardwareEnabled = false` |
| Entitlements | `RedMed.entitlements` empty `<dict/>` — no TAG, no applinks, no HealthKit |
| Usage string | `Info.plist` has no `NFCReaderUsageDescription` |
| HealthKit | `healthKitImportEnabled = false`; `HealthKitProfileImport.isAvailable = false`; no `import HealthKit` |
| Associated Domains | `associatedDomainsEnabled = false` |

Share/Pack is the honest owner NFC path while Write is parked. Storefront / dept “write from the app” stays off until Tag Reading + Write The Band on a blank NTAG216 (`docs/NFC-RESTORE.md`).

---

## Landed (small, safe)

| ID | Sev | Area | Evidence | Fix |
|----|-----|------|----------|-----|
| L1 | Medium | NFC honesty | `NFCView.linkStatus` showed **Band Written** / “Finish name, birth date, and blood type” whenever `braceletLinked` was true, even with hardware parked. `showsBraceletAsLinked` already requires `nfcHardwareEnabled`, so a leftover Keychain flag from an older hardware-on build (or a complete identity) still hit the incomplete-write copy. | Gate that branch on `nfcHardwareEnabled`. Parked NFC tab stays **Not Linked** + pack copy. Does not clear Keychain. Does not enable Write. |
| L2 | Medium | Notes / chip | Edit notes UI enforced 40 **words**. Save / codec already clip to `MAX_STR` **200 characters**. Forty longer words silently lost the tail on Save, so Keychain/chip could disagree with what the owner just typed. | `notesCharLimit = 200` in the editor (block keystroke/paste) + footer `words · chars`. Persist still clips. No wire-format change. |
| L3 | Low | Docs | `docs/AUDIT.md` still named SW cache `redmed-tapper-v152`. Tree is `v160` (`tapper/sw.js`, root `sw.js`, bundled copy — `cmp` equal). | Point the control table at `v160`. |
| L4 | Low | Docs | `docs/AUDIT.md` pointed the threat model at `docs/SECURITY.md`, which Max emptied (5462194). | Point at Help → Security. Do **not** restore SECURITY.md. |
| L5 | Low | Ads gate | `docs/ADVERTISING.md` said github.io is **404** until published. Live probe 2026-09-16: `https://roooted1776.github.io/tapper/` **HTTP 200**. `redmed.pages.dev/tapper/` still **404**. | Gate row now matches `docs/domain.md` + the probe. |

`node scripts/test-d-codec.mjs` — all OK after the Swift edits (codec file unchanged).

---

## Landed after this sweep (do not reopen)

| ID | Where | Note |
|----|-------|------|
| P2 | #618 dropped the Help cite; later pointer in `docs/SECURITY.md` | Security §7 no longer says “See `docs/SECURITY.md`.” Pointer file retargets to Help / `/Document/#security`. |
| P3 | #616 consent 4.17; #618 Document Version 4.17 | `ConsentSettings.currentVersion` and all Document Version lines match. No extra consent bump. |
| P4 | #618 | Security §6 heading is **HIPAA — not a covered entity**. Legal body unchanged. |

CI: `scripts/sync-document.sh` (via `scripts/sync-tapper.sh` on pages-deploy) greps the trio.

---

## Proposed (do not land without Max)

| ID | Sev | Area | Evidence | Ask |
|----|-----|------|----------|-----|
| P1 | Medium | Agent / GitHub surface | `README.md` is still empty on purpose (Max, 03e0617). `AGENTS.md` is restored. `docs/SECURITY.md` is a pointer into Help → Security (not the old dump). | Leave `README.md` empty unless Max says restore. |
| P5 | Medium | NFC restore gap | `NFCBandManager.verifyBand` has **no UI caller**. Hardware-on screen is Write + Preview (live RAM) + Load From Band (persists). No “read the chip, show the helper card, do not Save.” | When restoring Write, wire a verify/read-preview that does not `persist()`, or delete `verifyBand`. |
| P6 | Low | `#d=` lists | `joinList` uses `", "`; `splitList` splits on `,`. An allergy/med/condition containing a comma round-trips as two items. | Change delimiter only with tapper lockstep + a new codec test. Not this PR. |
| P7 | Low | Dead helper | `ProfileData.chip(from:)` never called; it omits `notes`. Live pack path `ProfileNFCCodec.chipProfile(from:)` **does** include notes. | Add notes or delete the helper if you touch that file. |
| P8 | Medium | Applinks restore | `BandTapIngress` requires `#d=` in `webpageURL`. iOS Universal Links often **drop the fragment**. Parked today (Safari `redmed://band#d=` keeps the hash). AASA files claim `33F9FQ4VBU.com.redmed.app`. | Before flipping `associatedDomainsEnabled`, confirm Team ID and a fragment-preserving handoff. Do not enable applinks in this PR. |
| P9 | Low | App Privacy | `PrivacyInfo.xcprivacy` collected-data array is empty; Connect draft is “Data Not Collected.” App uses When-In-Use location (on-device GPS + MapKit) and Face ID. | Counsel / Connect nutrition-label pass before submit. Not a runtime bug. |
| P10 | Low | MAX_STR units | Swift `clipStr` uses `String.count` (grapheme clusters). JS `slice(0, 200)` is UTF-16 code units. Emoji-heavy notes can clip differently. | Only matters at the 200 cap; leave unless you want a shared code-unit cap. |

---

## Checked and not bugs

| Claim | Result |
|-------|--------|
| PHI `print(` in Swift | Empty |
| Scanner writes owner Keychain | `persist()` / `adoptBandSnapshot` require `persists`; scanners get `snapshot()` |
| Linked from Share/Pack | `setBraceletPaired(true)` and `showsBraceletAsLinked` require `nfcHardwareEnabled`; parked `writeBand` never sets verified |
| Write button while parked | NFC tab primary CTA is Pack Band URL; Share Band URL + Preview; Load hidden; tab re-tap write gated |
| SOS autodial on band-tap auto-arm | Tapper: siren after `redmed://band` handoff; SOS tap autodials; crash wait 10s+30s; Preview `?src=app` skips auto-arm |
| Seizure timer autodial | Explicit Call button at 5:00; never autodials |
| Crash motion in background | `stopMonitoring()` on `.background`; keep-listening only `.inactive`; `audio` UIBackgroundMode is armed siren, not silent keep-alive |
| Passerby XSS on profile fields | `textContent`; hospital `innerHTML` goes through `esc()` |
| WK tapper script handlers | None. Help WK has `rmPolicy` only |
| `#d=` codec lockstep | `test-d-codec.mjs` OK (AES, notes index 12, amp strip, crash copy, local-only line) |
| Document host vs Xcode | After #593: `Document/index.html` = full policy; `Document/Document.html` = hash-preserving redirect. Intentional |
| github.io / pages.dev | `/tapper/` 200 on github.io; pages.dev 404 (known) |
| HIPAA / medical-device **marketing** | Banned in `DO-NOT.md` / FDA wall / App Store draft. Help legal text denies certification. Founder-why / PO rider stay in the denial lane |
| Fake App Store ID | `AppConfig.appStoreURL = nil` |
| HealthKit UI | Fill button hidden (`isAvailable == false`) |

Accepted residuals (already in `docs/AUDIT.md`): public AES packing key; Overpass POST of coordinates from the browser hospital search; CSP `unsafe-inline`; crash false positives; no background crash sensing.

---

## Explicitly not touched

- `nfcHardwareEnabled`, TAG entitlement, `NFCReaderUsageDescription`
- Write The Band / Load From Band UI
- HealthKit capability / `NSHealthShareUsageDescription`
- `associatedDomainsEnabled` / AASA / applinks
- HIPAA / FDA legal body in `Document.html`
- Restoring emptied `README.md`, or restoring the pre-5462194 `docs/SECURITY.md` dump (`docs/SECURITY.md` is a pointer now)

Ask Max before any of those.
