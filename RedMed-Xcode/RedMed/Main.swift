import SwiftUI

/// Owner app shell — edit profile, Aid treatments, NFC write, Find Help.
/// Product HTML is only the passerby `tapper.html` + policy pages; those redirect
/// here (`redmed://main`) for owner information.
struct Main: View {
    var body: some View {
        ContentView()
    }
}
